#include<iostream>
#include<fstream>
#include<vector>
#include<algorithm>
using namespace std;

int main() {
    // ifstream in ("paint.in");
    // ofstream out; out.open("paint.out");

    // int a, b, c, d; in >> a >> b >> c >> d;
    // if( (a<=c && c<=b) || (a<=d && d<=b) ) { //bwtn
    //     vector<int> n{a,b,c,d};
    //     sort(n.begin(), n.end());
    //     out << n[3]-n[0] << endl;
    // } else { // separate
    //     out << d-c + b-a <<  endl;
    // }

    freopen("paint.in", "r", stdin);
    freopen("paint.out", "w", stdout);

    int total = b-a + d-c;
}